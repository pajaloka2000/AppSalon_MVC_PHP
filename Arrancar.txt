1) para arrancar el servidor ingresamos a la carpeta public: cd public

2) luego inicamos el servidor: php -S localhost:3000

3) arrancamos el gulpfile fuera de public: npm run dev


PARA HACER EL DEPLYMENT DEL PROYECTO:

1) descargar: composer require vlucas/phpdotenv